<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Button
        android:text="Students"
        android:id="@+id/adm_main_btn_students"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="7dp"
        android:layout_margin="7dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/adm_main_btn_groups"
        android:onClick="getStudentsList"/>
    <Button
        android:text="Groups"
        android:id="@+id/adm_main_btn_groups"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="7dp"
        android:layout_margin="7dp"
        app:layout_constraintTop_toBottomOf="@+id/adm_main_btn_students"
        app:layout_constraintBottom_toTopOf="@+id/adm_main_btn_journals"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:onClick="getGroupsList"/>
    <Button
        android:text="Journals"
        android:id="@+id/adm_main_btn_journals"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="7dp"
        android:layout_margin="7dp"
        app:layout_constraintTop_toBottomOf="@+id/adm_main_btn_groups"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:onClick="getJournalsList"/>

</android.support.constraint.ConstraintLayout>